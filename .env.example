# PIN that locks the app (set this in Vercel → Settings → Environment Variables).
# If unset, the app is open with no login screen.
APP_PIN=1234

# Cloud storage (added automatically when you connect Upstash Redis in Vercel → Storage).
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
